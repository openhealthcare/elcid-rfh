angular.module('opal.services').factory('DateCreatedComparator', function($q, $http, $window, $log) {
    "use strict";

    var episodeDateComparator = function(someEpisode){
      debugger;
      return 1;
    };

    return [episodeDateComparator];
});
